name: Inspect translation generation

on:
  push:
    branches: ["diagnostic-pages"]
    paths:
      - ".github/workflows/inspect-translation-run.yml"

permissions:
  actions: read
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/github-script@v8
        id: inspect
        with:
          github-token: ${{ github.token }}
          script: |
            const runs = await github.paginate(
              github.rest.actions.listWorkflowRunsForRepo,
              { owner: context.repo.owner, repo: context.repo.repo, per_page: 100 }
            );
            const selected = runs
              .filter(run => run.name === 'Generate local translations')
              .slice(0, 5);
            const output = [];
            for (const run of selected) {
              const jobs = await github.paginate(
                github.rest.actions.listJobsForWorkflowRun,
                { owner: context.repo.owner, repo: context.repo.repo, run_id: run.id, per_page: 100 }
              );
              output.push({
                id: run.id,
                status: run.status,
                conclusion: run.conclusion,
                head_sha: run.head_sha,
                html_url: run.html_url,
                created_at: run.created_at,
                updated_at: run.updated_at,
                jobs: jobs.map(job => ({
                  id: job.id,
                  name: job.name,
                  status: job.status,
                  conclusion: job.conclusion,
                  steps: (job.steps || []).map(step => ({name: step.name, status: step.status, conclusion: step.conclusion}))
                }))
              });
            }
            require('fs').writeFileSync('translation-run-diagnostic.json', JSON.stringify(output, null, 2));
      - name: Commit diagnostic
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add translation-run-diagnostic.json
          git commit -m "Capture translation workflow status" || true
          git push origin HEAD:diagnostic-pages
